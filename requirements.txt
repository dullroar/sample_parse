# Core dependencies
click>=8.0.0

# Development dependencies (optional)
# Install with: pip install -e ".[dev]"
# Or manually: pip install pytest pytest-cov black flake8 mypy isort ruff
pytest>=7.0
pytest-cov>=4.0
black>=23.0
flake8>=6.0
mypy>=1.0
isort>=5.0
ruff>=0.1
